# DcisionAI Manufacturing Agent v1 Dependencies
# Production dependencies for AgentCore deployment

# AgentCore SDK
bedrock-agentcore>=0.1.2

# AWS dependencies
boto3>=1.39.7
botocore>=1.39.7

# FastAPI and server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Logging and monitoring
structlog>=23.2.0
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0

# Strands framework
strands-agents>=1.5.0
strands-agents-tools>=0.2.4

# AI/ML dependencies
openai>=1.0.0
anthropic>=0.7.0

# Basic optimization solvers (avoiding problematic ones)
scipy>=1.11.0
numpy>=1.24.0

# Optimization solvers (essential ones that work well)
ortools>=9.7.0
pulp>=2.7.0

# Additional optimization solvers for comprehensive coverage
highspy>=1.11.0
cylp>=0.93.0
cvxpy>=1.4.0
qpsolvers>=1.2.0
osqp>=0.6.2
clarabel>=0.5.0
scs>=3.2.4

# Note: Commercial solvers (gurobipy, cplex, mosek) require licenses
# Install separately if you have licenses: pip install gurobipy cplex mosek

# Async support
nest-asyncio>=1.5.0
aiohttp>=3.9.0

# System monitoring
psutil>=5.9.0
